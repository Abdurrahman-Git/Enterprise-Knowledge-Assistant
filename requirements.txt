fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# ── LLM & Embeddings ──────────────────────────────────────────────────────────
# LangChain: the orchestration framework that connects LLMs, vector stores, and tools
langchain==0.2.5
langchain-community==0.2.5
langchain-google-genai==1.0.6   # LangChain wrapper for Google Gemini

# Google Gemini (LLM + Embeddings)
google-generativeai==0.7.2

# ── Vector Database ───────────────────────────────────────────────────────────
# ChromaDB: an open-source vector database that runs locally (no server needed)
chromadb==0.5.3

# ── Document Processing ───────────────────────────────────────────────────────
# For reading different file types
pypdf==4.2.0           # PDF reading
python-docx==1.1.2     # Word document reading
python-multipart==0.0.9  # File uploads in FastAPI

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0
